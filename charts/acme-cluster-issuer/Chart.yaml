apiVersion: v2
appVersion: 1.13.3
version: 0.3.1
name: acme-cluster-issuer
type: application
description: |
  Deploys a production/staging pair of ACME cert-manager cluster issuer
  into a Kubernetes cluster. Cert-Manager must  already be installed and running prior
  to this chart being installed.
